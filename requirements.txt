# Core dependencies  
xmltodict>=0.9.0

# Environment-specific dependencies:
# - boto: provided by host environment (RDM-waterbutler or RDM-osf.io)

# Optional dependencies (installed based on use case)
# OSF Integration:
# Django>=3.2
# Flask>=1.0

# Waterbutler Integration:
# aiohttp>=3.8
# tornado>=6.0